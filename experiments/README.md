# experiments/

One subfolder per experiment run, named descriptively (e.g.
`gpt2-small-insertion-schedule-interleaved-freq10`, not `experiment_1`). Each
subfolder should point at (not duplicate) the corresponding `results/<run_name>/`
and `output/<run_name>/` artifacts, plus any experiment-specific notes/config diffs.
