# training_time/

Baseline class (iii): training-time interference (deduplication, reweighting, etc.)
layered on the `training/` pipeline (IMPLEMENTATION.md M7). Not yet implemented --
requires new training runs, to be scoped/confirmed before submission per CLAUDE.md §5.
