# slurm/

SLURM stdout/stderr for cluster jobs lands here (`%x-%j.out`, gitignored via `*.out`
in `.gitignore`). No job scripts are checked in yet -- `*.sbatch` is gitignored
project-wide; a sample header file will be added once cluster account/partition
details are confirmed.
